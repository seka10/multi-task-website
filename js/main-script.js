
function phones(phoneSrc) {
    const img = document.querySelector(".image"); // تأكد إنك بتجيب الصورة جوا الفانكشن
    img.src = phoneSrc;
  }
  const container = document.querySelector('.container');

  function colors(color) {
    container.style.background = color;}